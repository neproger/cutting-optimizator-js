# Проблемы и улучшения (чек-лист)

Формат статуса:
- [ ] не исправлено
- [x] исправлено

## P0 — критично

- [x] Сделать `packSheets` синхронной (убрать `async`) и обновить контракт в README/тестах.
- [x] Убрать побочный `console.error` из валидации (или сделать опциональный `debug`/`onError`).
- [x] Разделить `tooBigParts` и `unplacedParts` (не смешивать oversized и неупакованные эвристикой).
- [x] Пересмотреть guard в 2D-цикле, чтобы избежать преждевременной остановки упаковки.
- [x] Добавить явную причину остановки (`stopReason`) при выходе по guard/лимитам.

## P1 — высокий приоритет

- [x] Унифицировать сравнения размеров через `EPSILON` во всех fit-проверках.
- [x] Убрать потерю точности из-за `Math.floor` в DP (или ввести масштабирование размеров).
- [x] Добавить `settings.minLeftoverSize` и фильтрацию слишком маленьких остатков.
- [x] Исправить 1D-режим: убрать жестко заданный `profileWidth = 200` и использовать `stock.width`.
- [x] Убедиться, что 1D-карта корректно учитывает `stock.width`.
- [x] Оптимизировать повторные сортировки кандидатов (`getSortedTargetCandidates`) в 2D-проходах.
- [x] Пересмотреть удаление детали из пула: не использовать fallback только по `id` (риск удаления не того экземпляра).

## P2 — средний приоритет

- [x] Добавить в результат `stats` (`iterations`, `usedArea`, `efficiency`, `maxRecursionDepthReached` и т.д.).
- [x] Добавить индикатор достижения лимита рекурсии в `runTargetPass` (без обязательного лог-шума).
- [x] Убрать магические параметры 1D: `profileWidth` всегда из `stock.width`, `profileSideOffset` удален из API.
- [x] Добавить JSDoc для ключевых функций (особенно DP и эвристик).
- [x] Добавить валидацию/ограничения на экстремально большие размеры (защита от некорректного ввода).

## Тесты (обязательно покрыть)

- [x] Большие наборы деталей (1000+).
- [x] Проверка отсутствия пересечений (`overlap`) между размещенными деталями.
- [x] Кейсы с дробными размерами (`float`) и стабильностью fit-проверок.
- [x] Сценарий `sheetTrim` больше доступного листа.
- [x] Граничные случаи `kerf` (включая близкие к размеру детали).
- [x] Кейс с одинаковыми/повторяющимися `id` деталей.
- [x] Проверка корректности `percentage` и `usedArea`.
- [x] Проверка, что деталь не размещается дважды.

## Документация и DX

- [x] Синхронизировать README с фактическим пакетом и API.
- [x] Добавить раздел про диагностические поля результата (`stopReason`, `stats`, `unplacedParts`).

